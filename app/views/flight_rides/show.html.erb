<p> Shows all flight_Ride</p>

 <p> This is the users show page, List result or tickets after the went on the ride</p>
<header class="header">
    
          User: <h3><%= @user.name %><br></h3>
    <br>
    <h4> Flight_Ride Iternary</h4>
<br>
   
    <% @user.flight_rides.each do |user_flight_ride|%>

     <br>
     <p>flight_ride_id</p>
     <h3> <%= user_flight_ride.id%></h3>

      <p>flight_class</p>
     <h3> <%= user_flight_ride.flight_class%></h3>

     <p>flight_id </p>
     <h3> <%= user_flight_ride.flight_id%></h3>
<br>
   <p><%= link_to "Edit FlightRide", edit_user_flight_ride_path(current_user) %></p>

<br>
   <p><%= link_to "Delete This Flight", user_flight_ride_path(current_user) %></p>
    <%end%>

  <br>
      <h2><%= link_to "See all Flights", flights_path %></h2>

        <% if flash[:notice] %>
         <div class="notice"><%= flash[:notice] %></div>
        <% end %>
   <br>
    
   <br>

     <%=link_to "Log Out", logout_path, :method => :delete %>
        <%= hidden_field_tag :user_id, current_user.id %>
  <br>
   OR
  <br>

        <% if auth_hash = request.env["omniauth.auth"] %>
        <h1><%= @user.name %></h1>
        <h2>Email: <%= @user.email %></h2>
        <h2>Facebook UID: <%= @user.uid %></h2>
        <img src="<%= @user.image %>">
        <% else %>
        <%= link_to('Log in with Facebook!', '/auth/facebook') %>
        <% end %>

  </header>

  