 <P> show all the flights.</P>

   <h1> This is the Flight index page, Lists all the Flight.. </h1>

  <% @flights.each do |flight| %>

  <br/>
   
     <h3> <%= link_to "Go on flight #{flight.id}", flight_path(flight)%></h3> <br />

     <p> flight inspection </p>
     <h3> <%= flight.inspection%></h3>

     <p> flight fuel_cost</p>
     <h3> <%= flight.fuel_cost%></h3>

     <p> flight destination</p>
     <h3> <%= flight.destination%></h3>

     <p> flight flight_departure</p>
     <h3> <%= flight.flight_departure%></h3>

     <p> flight flight_sit</p>
     <h3> <%= flight.flight_sit%></h3>

<% end %>
<br>
<br>
#  It not displaying current_user...???
     <%= hidden_field_tag :user_id, current_user.id %>

    <%= hidden_field_tag :name => request_forgery_protection_token.to_s, :value => form_authenticity_token %>
   
        <h3><%= link_to "Create a New flight", new_flight_path %></h3>
    # want user that is logged in to create a new flight with flight_ride classes etc.
